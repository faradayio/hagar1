<% @repos.each do |repo| %>
if [ ! `which <%= repo %>` ]
then
  alias <%= repo %>="cd ~/<%= repo %>"
fi
<% end %>

alias ku='unicornid=$(ps ax | grep unic | grep master | grep -v grep | cut -f1 -s -d" "); if [ $unicornid ]; then kill $unicornid; else echo Already dead; fi'
